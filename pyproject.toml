[tool.poetry]
name = "balena-update-bundle-pwn"
version = "0.1.0"
description = "pwn"
authors = ["pwn <pwn@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
